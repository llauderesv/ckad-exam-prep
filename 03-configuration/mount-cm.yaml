apiVersion: v1
kind: Pod
metadata:
  name: test-po2
spec:
  containers:
    - name: test-po2
      image: nginx:1.19.0
      volumeMounts:
        - mountPath: /etc/config
          name: config-vol
  volumes:
    - name: config-vol
      configMap:
        name: db-config-file
